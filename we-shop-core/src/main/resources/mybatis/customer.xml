<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="shell.we.shop.mapper.CustomerMapper">

    <select id="getCustomerList" resultType="shell.we.shop.dao.Customer">
        select * from customer order by id
    </select>

</mapper>